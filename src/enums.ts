export enum ParameterErrors {
    UNDEFINED_PARAMETERS = "0001; existem parametros não definidos",
    INVALID_PARAMETERS = "0002; tipagem de parametros invalida",
}

export enum ConfigErrors{
    UNDEFINED_CONFIG_FIELD = "1111; configuraçao invalida"
}